package com.zaineedyou.val0x07.keybind;

import net.minecraft.client.option.KeyBinding;
import net.minecraft.util.Identifier;

/** Satu-satunya pendaftaran kategori Controls untuk seluruh keybind Val0x07. */
public final class Val0x07KeybindCategory {
    public static final KeyBinding.Category CATEGORY =
            KeyBinding.Category.create(Identifier.of("val0x07", "main"));

    private Val0x07KeybindCategory() {
    }
}
